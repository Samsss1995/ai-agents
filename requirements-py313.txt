# 🌙 Moon Dev's AI Trading Agents Requirements 🌙
# Built with ❤️ by Moon Dev

# ========== Core AI/LLM Dependencies ==========
anthropic==0.40.0
openai==1.59.5  # Also used for xAI (Grok) integration
groq==0.16.0
google-generativeai==0.8.3
# Note: xAI/Grok uses OpenAI client - no separate package needed
# Note: google-generativeai may have protobuf conflicts
# If issues occur, use: protobuf==4.25.1

# ========== Crypto/Trading Dependencies ==========
# Solana
solana==0.35.1
solders==0.21.0

# HyperLiquid (Updated for v0.20.0)
hyperliquid-python-sdk==0.24.0
eth-account==0.11.0
web3==6.20.1

# ========== Data & Analysis ==========
pandas-ta==0.4.71b0
TA-Lib==0.6.8  # Technical indicators
Backtesting==0.6.5  # Backtesting framework
yfinance==0.2.43  # For fetching Yahoo Finance data

# ========== Utilities ==========
python-dotenv==1.0.0
termcolor==2.3.0
colorama==0.4.6  # For colored terminal output
requests==2.31.0
tqdm==4.67.1
schedule==1.2.0  # For scheduled tasks
psutil==5.9.8  # System and process utilities
asyncio  # Built-in, but noting for async support
httpx==0.28.1  # Modern HTTP client



# ========== Optional/Advanced ==========
# Uncomment if needed for specific agents:
# jupyter==1.0.0  # For notebook support
# beautifulsoup4==4.12.0  # For web scraping
# selenium==4.15.0  # For browser automation
# playwright==1.40.0  # Alternative browser automation

# ========== Notes ==========
# Python version: 3.10.9 recommended
# Environment: Use conda environment 'tflow'
#
# API Keys Required (in .env file):
# - ANTHROPIC_KEY (Claude)
# - OPENAI_KEY (GPT-4)
# - GROK_API_KEY (xAI Grok - uses OpenAI client)
# - GROQ_API_KEY (Groq)
# - GEMINI_KEY (Google)
# - DEEPSEEK_KEY (DeepSeek - if using)
# - BIRDEYE_API_KEY (Solana data)
# - MOONDEV_API_KEY (Custom signals)
# - SOLANA_PRIVATE_KEY (Trading)
# - HYPER_LIQUID_KEY (HyperLiquid)
#
# To install all requirements:
# pip install -r requirements.txt
#
# For TA-Lib on Mac:
# brew install ta-lib
# pip install TA-Lib
#
# For TA-Lib on Linux:
# wget http://prdownloads.sourceforge.net/ta-lib/ta-lib-0.4.0-src.tar.gz
# tar -xzf ta-lib-0.4.0-src.tar.gz
# cd ta-lib/
# ./configure --prefix=/usr
# make
# sudo make install
# pip install TA-LibPyYAML==6.0.2  # configs/research_factory.yaml et al
yfinance==1.4.1  # daily data for stocks/indices/commodities/forex (research factory)
